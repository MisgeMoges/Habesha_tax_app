import 'package:flutter/material.dart';

class AppColor {
  // Primary brand color (deep green, trust/finance)
  static const Color appColor = Color(0xFF17494D);

  // Banner/header color (slightly lighter, harmonious with primary)
  static const Color appBannerColor = Color(0xFF206A5D);

  // Button color (vivid accent, stands out but still professional)
  static const Color appButton = Color(0x626200EB);

  // Button text color (dark for contrast on yellow)
  static const Color appButtonText = Color(0xFF222222);

  // Container/background color (very light, almost white)
  static const Color appContainerColor = Color(0xFFF5F7FA);

  // Main font color (dark, for readability)
  static const Color appFont = Color(0xFF222222);

  // Subdued/secondary text color (for less important text)
  static const Color subColor = Color(0xFF6C757D);

  // Error color (for warnings, errors, etc.)
  static const Color error = Color(0xFFD7263D);

  // Success color (for success messages, etc.)
  static const Color success = Color(0xFF21BF73);
}
