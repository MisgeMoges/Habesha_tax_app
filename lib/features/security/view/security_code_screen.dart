import 'package:flutter/material.dart';

class SecurityCodeScreen extends StatelessWidget {
  SecurityCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Security Code'), centerTitle: true),
      body: const Center(child: Text('Security Code Settings Coming Soon...')),
    );
  }
}
